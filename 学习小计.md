# 我的 AI 全栈学习笔记

## 📖 目录

- [2026-02-11 MySQL 建表踩坑](#2026-02-11-mysql-建表踩坑)
- [2026-02-11 结构化输出](#2026-02-11-结构化输出)
- [2026-02-25 结构化输出](#2026-02-25-结构化输出)
- [2026-02-27 跑起来自动cli工具--mini-cursor](#2026-02-27-跑起来自动cli工具--mini-cursor)
- [2026-02-28 流式版mini-cursor](#2026-02-28-流式版mini-cursor)
- [2026-03-03 提示词模板prompt template](#2026-03-03-提示词模板prompt-template)
- [2026-07-05 Nest + LangChain 实现基于 SSE 的流式 AI 接口](#2026-07-05-nest--langchain-实现基于-sse-的流式-ai-接口)

## 2026-02-11 MySQL 建表踩坑

- 问题：从公众号复制代码导致 `\u00A0` 空格报错
- 解决：用 `cleanSQL()` 清理非法字符或者直接去github里面复制
- 教训：**永远不要直接粘贴富文本代码**，光哥里面用的是 不间断空格（No-break space）Unicode：\u00A0

### 代码示例

```js
function cleanSQL(str) {
  return str.replace(/\u00A0/g, ' ').trim();
}
```

## 2026-02-11 结构化输出

### npm和pnpm的区别

1. npm 是 Node.js 官方默认的包管理器；
2. pnpm 是一个更快、更节省磁盘空间的第三方包管理器，通过“硬链接 + 符号链接”实现高效依赖管理

---

## 2026-02-25 结构化输出

### 结构化输出的方式

1. JsonOutputParser (基础版)

原理：在 Prompt 中插入简单的 JSON 格式指令，拿到结果后直接 JSON.parse()。
特点：
    ❌ 无类型校验：只保证是 JSON，不保证字段对不对。
    ❌ 脆弱：LLM 多输出一句“好的，这是 JSON”，就会解析报错。
    ✅ 适用：极简场景，或你完全掌控 Prompt 且模型很听话时。
2. StructuredOutputParser (增强版)

原理：基于 Zod Schema 自动生成详细的 Prompt 指令，并在解析时进行类型校验。
特点：
    ✅ 强约束：通过 Prompt 强力引导模型输出特定结构。
    ✅ 类型安全：解析失败会抛出具体错误。
    ✅ 支持真流式：配合 stream() 可实现逐字/逐字段输出。
    ⚠️ 依赖模型配合：本质还是靠 Prompt 哄模型，偶尔会翻车。
    ✅ 适用：模型不支持 Tool Calls 时的首选方案，或需要真流式展示 JSON 时。
3. Tool Calls (原生能力)

原理：利用模型原生的 Function Calling 能力，将 Schema 定义为工具参数。
特点：
    ✅ 最可靠：模型经过微调，几乎 100% 按格式输出，无需 Prompt 哄骗。
    ✅ 语义清晰：明确区分“思考”和“行动”。
    ❌ 非流式友好：原生返回的是完整对象，无法逐字展示。
    ✅ 适用：后端数据处理、Agent 决策、对准确性要求极高的场景。
4. withStructuredOutput (智能路由 - 推荐默认使用)

原理：LangChain 的“大管家”。接收 Zod Schema，自动判断模型能力：
    优先：若模型支持 Native JSON Schema → 用最强约束模式。
    次选：若模型支持 Tool Calls → 伪装成工具调用。
    兜底：若都不支持 → 降级为 StructuredOutputParser。
特点：
    ✅ 开发体验最佳：一行代码搞定，不用关心底层。
    ⚠️ 流式局限：在前两种模式下，必须等完整 JSON 生成并校验通过后才返回（表现为“单一大 Chunk”），无法实现逐字打字机效果。
    ✅ 适用：90% 的非流式结构化需求。
5. JsonOutputToolsParser (流式神器)

原理：专为 Tool Calls + Streaming 设计的解析器。能在 token 流中实时拼接并检测完整的 JSON 参数。
特点：
    ✅ 实时响应：一旦模型生成完工具参数（哪怕后续话还没说完），立刻解析并触发工具调用。
    ✅ 用户体验：可实现“正在查询...”的即时反馈，无需等待模型说完所有话。
    ✅ 适用：需要流式展示 + 实时调用工具的高级场景。

## 2026-02-27 跑起来自动cli工具--mini-cursor(本质就是 stdin 读取)

1. npm run dev、pnpm install、vite、通常是在终端里跑。终端本身就是 shell。mini-cursor 是：程序内部调用子进程，这时候：直接绕过了终端。所以必须自己处理 shell 解析问题。

2. -- --template react-ts  第一个 -- 告诉 pnpm：后面的参数传给 create-vite，而不是传给 pnpm，告诉vite直接使用用户指定模板，就可以直接跳过交互问题。

## 2026-02-28 流式版mini-cursor

流式打印 tool 的参数，需要做好 AIMessageChunk 的 concat，以及用 JsonOutputToolsParser 做 tool_call_chunks 的解析，之后增量打印

## 2026-03-03 提示词模板prompt template

> 结合 Milvus 向量数据库 + LangChain 实战总结，聚焦 Few-Shot 语义示例选择

---

### 🧩 核心模板类型与用途

| 模板类型 | 作用 | 在本项目中的用途 | 关键特点 |
|--------|------|------------------|--------|
| **`PromptTemplate`** | 基础字符串模板，支持 `{变量}` 占位 | 构建周报主提示词，如 `“请根据以下内容写周报：{content}”` | 简单直接，适合单轮生成 |
| **`ChatPromptTemplate`** | 对话式模板（system/user/assistant 消息数组） | 让 LLM 扮演“周报助手”，统一语气和格式 | 更符合现代 LLM 的训练范式 |
| **`FewShotPromptTemplate`** | 自动生成带示例的完整提示词 | **核心组件**：注入从 Milvus 检索出的周报样例 | 显著提升输出风格一致性 |
| **`FewShotChatMessagePromptTemplate`** | Few-Shot 的对话形式（LangChain 中常通过组合实现） | 若需多轮对话+示例引导（如先展示例子再提问） | 融合角色设定与示例引导 |
| **`LengthBasedExampleSelector`** | 根据输入长度动态控制示例数量 | 防止提示词过长（如用户输入很长时，只选 2 个例子） | 控制 token 消耗，避免超限 |
| **`SemanticSimilarityExampleSelector`** | **已实现**<br>基于向量检索，选出语义最相近的示例 | 用户说“我们在做技术债”，自动匹配“重构+补测试”的周报样例 | **大幅提升输出相关性 ✅** |
| **`PipelinePromptTemplate`** | 组合多个子模板为一个完整 prompt | 拼接 “角色设定” + “动态示例” + “用户输入” | 模块化设计，易于维护和调试 |

---

## 2026-07-05 Nest + LangChain 实现基于 SSE 的流式 AI 接口

*(待学习整理...)*

### Generator 的双向通信机制

- `yield` 是双向通道：
  - **向外**：`yield 值` → 把值吐出去，放在 `{value: xxx}` 里
  - **向内**：`next(x)` 的 `x` → 成为上一个 `yield` 表达式的结果，被变量接收
- 关键代码理解：
  ```js
  function* gen(val) {
    const val = yield val;  // 第一次 next() 吐参数 val，第二次 next(x) 的 x 赋值给 const val
    yield val;              // 吐刚刚接收到的 x
    yield 3;                // 直接吐 3
  }
  const g = gen(10)
  g.next()       // {value: 10, done: false}  — 第一次执行到 yield val，吐参数 10
  g.next(45)     // {value: 45, done: false}  — 45 变成 yield 表达式结果赋给 const val，然后 yield val 吐 45
  g.next(999)    // {value: 3, done: false}   — 999 变成第二个 yield 表达式结果但没人接，然后 yield 3 吐 3
  ```
- **双向通信的含义**：普通函数一旦开始跑就不能从外部往里塞数据；生成器可以暂停 → 与外界交换数据 → 继续跑
- `yield 值` 是一个表达式，它的值由下一个 `next(x)` 的 `x` 决定

## Generator（生成器）与 yield 概念

- **生成器函数**：函数名后面加 `*`（如 `async *streamChain()`），表示这是一个生成器函数
- **`yield` vs `return`**：
  - `return`：函数结束，一次性返回整个值（必须所有数据准备好才能返回）
  - `yield`：逐个吐出数据，每次 yield 后函数暂停，等调用方 next() 再继续（来一点给一点）
- **Generator 里的 `return`**：不是不能用，但语义变了——它作为迭代结束时 `{ value: xxx, done: true }` 的最终值，`for await...of` 循环取不到这个值
- **`async *` 才是能用 `yield` 的关键**，`AsyncGenerator<string>` 只是 TS 类型标注
- **为什么 SSE 流式需要 yield**：AI 每生成一个字就 yield 出去，用户不用等全部说完才看到结果，体验像 ChatGPT 打字一样
- **消费生成器的方式**：
  - `for await (const chunk of gen)` — 最常用
  - `await gen.next()` — 手动控制，每次取一个值

## Nest 模块作用域与 isGlobal

- Nest 模块默认是**封闭的**：A 模块 import 了某个模块，只有 A 模块内部能用它的服务
- **`isGlobal: true`**（如 `ConfigModule.forRoot({ isGlobal: true })`）让该模块变成**全局模块**
- 全局模块被注册后，**所有模块不需要再次 import** 就能直接用它的服务（如 `ConfigService`）
- 底层等价于给模块加 `@Global()` 装饰器
- 使用建议：
  - ✅ 公共基础设施（Config、Logger、数据库）→ 设 `isGlobal: true`，省事
  - ❌ 业务模块（BookModule、UserModule）→ 不要全局，显式 import 更清晰
  - ⚠️ 全局模块不宜过多，否则依赖关系不透明

## Nest RESTful 路由匹配规则

- `nest g res` 生成的 Controller 默认遵循 RESTful 规范
- 路由映射规则：
  - `@Get()` → `findAll()` — 查列表，路径为 `GET /资源名`
  - `@Get(':id')` → `findOne()` — 查单个，路径为 `GET /资源名/:id`
  - `@Post()` → `create()` — 创建
  - `@Patch(':id')` → `update()` — 更新
  - `@Delete(':id')` → `remove()` — 删除
- 访问 `GET /ai` 匹配到 `@Get()`（无路径参数），所以走 `findAll()`
- 访问 `GET /ai/1` 匹配到 `@Get(':id')`，走 `findOne(1)`
- 这不是"默认调用"，而是 Nest 的路由匹配机制决定的
- 这些命名是 RESTful 的行业习惯，不只是 Nest 特有

## Nest 框架基础（本节理论部分）

1. **Nest 是什么**：Node.js + TypeScript 的最主流后端框架，底层默认基于 Express
2. **MVC 概念**：Model - View - Controller 三层架构
   - Controller（控制器）：接请求、返回响应，不做业务逻辑
   - Service/Model（服务）：真正的业务逻辑（调 AI、查数据库）
   - View（视图）：数据怎样展示（JSON / SSE 流）
   - 比传统 MVC 多了 Module（模块）层，把 Controller + Service 打包
3. **Nest 核心文件职责**：
   - `main.ts` — 入口，NestFactory.create(AppModule) 启动应用
   - `app.module.ts` — 根模块，声明 controllers 和 providers
   - `app.controller.ts` — @Controller() + @Get() 定义路由
   - `app.service.ts` — @Injectable() 业务逻辑
4. **依赖注入（DI）**：不需要自己 new，Nest 自动注入实例。`constructor(private readonly appService: AppService) {}`
5. **nest g res 命令注意点**：
   - 必须在项目根目录（有 nest-cli.json 的目录）下运行，否则生成路径会错
   - `nest g res xxx` 只生成 Resource 文件，**不会自动注册到 app.module.ts**
   - 需要手动在 app.module.ts 的 imports 数组添加，或单独跑 `nest g module xxx`
   - 示例：`cd hello-nest-langchain && nest g res book --no-spec`，然后手动 `import { BookModule } from './book/book.module'` 在 `@Module({ imports: [BookModule] })`
6. **脚本模式 vs Nest 后端模式对比**：
   - 脚本：`node xxx.js` 跑一次结束，命令行交互
   - Nest：常驻服务器，HTTP 接口，多用户并发，分层清晰

### 自定义 Provider 与 @Inject 注入

- **自定义 Provider**：在 Module 的 providers 中用对象语法注册的服务
  ```ts
  {
    provide: 'CHAT_MODEL',              // 令牌（字符串标识）
    useFactory: (configService) => {    // 工厂函数，生产实例
      return new ChatOpenAI({...});
    },
    inject: [ConfigService],            // 工厂函数需要其他依赖时声明
  }
  ```
- **`@Inject('CHAT_MODEL')`**：在构造函数中通过令牌索取实例
  ```ts
  constructor(@Inject('CHAT_MODEL') model: ChatOpenAI) {}
  ```
- **为什么需要这种模式**：
  - `ChatOpenAI` 是第三方包（@langchain/openai）的类，没有 `@Injectable()` 装饰器，Nest 不认识它的类型
  - 需要用字符串令牌 `'CHAT_MODEL'` 做中介来注册和注入
- **对比两种注入方式**：
  - 按类型注入（省略 @Inject）：`constructor(private readonly appService: AppService) {}` — 用于有 `@Injectable()` 的自家服务
  - 按令牌注入（写 @Inject）：`@Inject('CHAT_MODEL') model: ChatOpenAI` — 用于第三方类或需要自定义创建逻辑的场景
- **好处**：
  - 配置集中：API Key 等配置只在 Module 的工厂函数里管理
  - 方便替换：换模型只改 Module，Service 不用动
  - 拿到 ConfigService：工厂函数可注入其他服务读取配置

### Nest 模块导入顺序的坑：ConfigModule 必须在业务模块之前

- **问题现象**：SSE 请求一直显示"连接中..."，没有任何报错
- **根因**：`app.module.ts` 中 `imports` 数组的顺序问题
  - ❌ 错误顺序：`BookModule, AiModule, ConfigModule.forRoot()`
  - ✅ 正确顺序：`ConfigModule.forRoot(), BookModule, AiModule`
- **为什么**：Nest 按 `imports` 数组顺序依次初始化模块。`AiModule` 的 `useFactory` 中用到了 `ConfigService.get('OPENAI_API_KEY')`，如果 `ConfigModule` 排在后面，.env 还没加载，`get()` 返回 `undefined`，导致 `ChatOpenAI` 创建时缺少 API Key
- **教训**：依赖配置的模块，必须把 `ConfigModule.forRoot()` 放在它们前面。就算在某个模块里 `isGlobal: true`，也要注意初始化顺序
- **注意**：`isGlobal: true` 只是让其他模块**能用** ConfigService，但不改变模块本身的**初始化时机**
